import * as R from 'ramda';

export const { values } = R;
export const { isEmpty } = R;
export const { isNil } = R;
export const { keys } = R;
export const { equals } = R;
export const { propEq } = R;
export const { entries } = Object;
export const { prop } = R;
export const { has } = R;
